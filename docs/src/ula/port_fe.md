# Port 0xFE (Border, Sound, Keys)

TODO: Document OUT to port `0xFE` — the three bits that control the border colour, the MIC and EAR bits for tape/audio, and how keyboard row selection works with IN from port `0xFE`. Include a bitfield diagram.
