# Video Memory Layout

TODO: Document the unusual screen memory layout. Explain why consecutive pixel rows are spaced 256 bytes apart instead of 32, the three thirds structure, and how to compute screen address from (x, y) coordinates. Include a reference diagram or lookup table.
