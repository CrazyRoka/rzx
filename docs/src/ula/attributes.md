# Attributes & Flashing

TODO: Document the attribute byte format (INK, PAPER, BRIGHT, FLASH), the attribute file area (`0x5800 – 0x5AFF`), the colour palette mapping (RGB bits to actual colours), and the flashing mechanism timing (rate, synchronisation to frame). Include the colour index table.
