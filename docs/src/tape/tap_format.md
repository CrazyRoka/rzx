# The .TAP Format

TODO: Document the .TAP file format — the simple block-based format containing raw ROM-saved data blocks. Describe the header block (flag byte, filename, length, parameters, checksum) and data block structure. Provide a byte-level specification table.
