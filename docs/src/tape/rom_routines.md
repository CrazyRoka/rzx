# LOAD/SAVE ROM Routines

TODO: Document the key ROM routines used for tape I/O — SA-BYT (save a byte), LD-BYT (load a byte), SA-VER (verify), and the higher-level calls that orchestrate header and data block transfer. List entry addresses and register parameters.
