# The .TZX Format

TODO: Document the .TZX format — the extended block-based format that stores exact pulse timings. List the common block types (standard speed, turbo speed, pure tone, pulse sequence, etc.) and their binary structure. Reference the official specification.
