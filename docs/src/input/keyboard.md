# Keyboard Matrix

TODO: Document the Spectrum's keyboard matrix — the 8×5 row/column layout, the port address used for each row (half-row decoding), and the mapping from key press to port read value. Include a reference table.
