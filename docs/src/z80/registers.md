# Registers & Flags

TODO: Document the Z80 register set — main registers (AF, BC, DE, HL, IX, IY, SP, PC), shadow registers (AF', BC', DE', HL'), special-purpose registers (I, R), and the flag register layout (S, Z, H, P/V, N, C).
