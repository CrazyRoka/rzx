# Instruction Set

TODO: Document the Z80 instruction set — opcode maps (CB, DD, ED, FD prefixed), instruction timings in T-states, and any Spectrum-specific behaviour (e.g. contention during M1 cycles).
