# Contended Memory

TODO: Detail the ULA/CPU bus contention mechanism. Explain which address regions are contended, the exact T-state timing of contention during each scanline, and how this affects instruction execution times. Provide a reference table of instruction delays per T-state for accurate cycle-stepped emulation.
