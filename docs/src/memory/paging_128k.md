# 128K Memory Paging

TODO: Document the memory paging mechanism introduced in the 128K models. Cover the paging port (`0x7FFD`), the RAM bank selection (banks 0–7), the screen buffer selection (bank 5 or 7), the ROM selection (128K ROM vs. 48K ROM), and the differences between 128K/+2 and +3 paging (including the +3's additional port `0x1FFD` for special paging modes).
